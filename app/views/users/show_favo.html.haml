- provide(:title, @user.name)
.container
  .main_view
    .row
      .col-lg-8
        %h3= @title
        - if @user.favos.any?
          %ol.posts
            = render @posts
          = will_paginate @posts
        - else
          %h3.text-center まだお気に入りに追加された投稿はありません！
      %aside.col-lg-4
        %section.user_info
          = render 'shared/user_info'
        %section.stats
          = render 'shared/stats'