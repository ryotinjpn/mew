<!--フォローとフォロワーの一覧表示-->
<% provide(:title, @title) %>
<div class="container">
  <div class="main_view">
    <div class="row">
      <div class="col-lg-9">
        <h3><%= @title %></h3>
        <% if @users.any? %>
          <ul class="users follow">
            <%= render @users %>
          </ul>
          <%= will_paginate %>
        <% end %>
      </div>
      <aside class="col-lg-3">
        <section class="user_info">
          <%= render 'shared/user_info' %>
        </section>
        <section class="stats">
          <%= render 'shared/stats' %>
        </section>
      </aside>
    </div>
  </div>
</div>