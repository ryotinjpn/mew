= form_with(model: current_user.like_relationships.build, class: "like" ,remote: true ) do |f|
  %div= hidden_field_tag :post_id, post.id
  = button_tag(class: "btn btn-default" ) do
    = content_tag :span, post.liked_by.count, class: "glyphicon glyphicon-heart-empty"
