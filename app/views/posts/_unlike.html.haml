= form_with(model:current_user.like_relationships.find_by(post_id: post.id), |
  html: { method: :delete }, class: "like", remote: true) do |f|             |
  = button_tag( class: "btn btn-default") do
    = content_tag :span, post.liked_by.count, class: "glyphicon glyphicon-heart"
