= form_with(model:current_user.favorite_relationships.find_by(post_id: post.id),html: { method: :delete }, class: "favo", remote: true) do |f|
  = button_tag( class: "btn btn-default") do
    = content_tag :span, post.favoed_by.count, class: "fas fa-bookmark"
