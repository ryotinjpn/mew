= form_with(model: current_user.favorite_relationships.build, class: "favo" ,remote: true ) do |f|
  %div= hidden_field_tag :post_id, post.id
  = button_tag(class: "btn btn-default" ) do
    = content_tag :span, post.favoed_by.count, class: "glyphicon glyphicon-heart-empty"
